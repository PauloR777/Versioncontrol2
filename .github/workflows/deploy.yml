name: CI/CD Pipeline for Quasar
on:
  push:
    # 📌 ตรวจสอบให้แน่ใจว่าเป็น branch ที่โค้ดต้นฉบับอยู่
    branches: [ "master", "main" ]
  pull_request:
    branches: [ "master", "main" ]
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Cache node_modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - uses: actions/setup-node@v4
        with:
          node-version: '20' # ใช้ Node.js เวอร์ชัน 20

      - name: Install and Build
        run: |
          npm install
          # 💡 แก้ไข: เพิ่ม --public-path เพื่อกำหนดเส้นทางสำหรับ GitHub Pages
          npx quasar build --public-path /Versioncontrol2/

      - name: Deploy to GitHub Pages
        # 💡 ไม่จำเป็นต้องมีเงื่อนไข if: เพราะ action นี้จะทำงานก็ต่อเมื่อมีการ push เท่านั้น
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist/spa
          # publish_branch: gh-pages # ไม่ต้องระบุ หากต้องการให้ deploy ไปที่ branch ที่ตั้งค่าไว้ใน Settings