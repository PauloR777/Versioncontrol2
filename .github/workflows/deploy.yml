name: Deploy Quasar SPA to GitHub Pages

on:
  push:
    # 📌 ตรวจสอบ branch ที่โค้ดต้นฉบับอยู่ (อาจเป็น master, main, หรือ gh-pages)
    branches: [ "master" ] 

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write 

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build Quasar SPA with Base Path
        # บังคับ Base Path ให้ตรงกับชื่อ Repository (ต้องมี / นำหน้าและต่อท้าย)
        run: npx quasar build --public-path /Versioncontrol2/ 

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist/spa